# Server
server.port=8080

# Database - uses Render environment variables
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.huihouqwgdtoxqnyvitz.supabase.co:5432/postgres?sslmode=require}spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Cdnw3cY090lL4S8F}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=${JWT_SECRET:savadhika_secret_key_2024}
jwt.expiration=86400000

# Logging
logging.level.com.savadhika=DEBUG
logging.level.org.springframework.web=INFO
# Allow app to start even if DB is not available
spring.jpa.hibernate.ddl-auto=none
spring.datasource.hikari.initialization-fail-timeout=-1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=2
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never